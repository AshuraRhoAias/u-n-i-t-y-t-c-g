using UnityEngine;

namespace WaifuSummoner.Cards
{
    public enum Rarity
    {
        Common,     // Normal cards
        Spicy,      // Rare (R)
        Naughty,    // Super Rare (SR)
        Radiant     // Ultra Rare (UR)
    }
}